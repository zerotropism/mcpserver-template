[project]
name = "fastmcptest"
version = "0.1.0"
description = "Simple MCP server in Python w/ FastMCP"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "fastmcp>=3.1.0",
    "python-dotenv>=1.2.2",
]
